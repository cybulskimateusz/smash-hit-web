# Components

ECS components - lightweight data containers attached to entities.
Each component defines a specific capability or property (e.g. `Transform`, `RigidBody`, `Collider`).

Components hold data only; logic lives in `systems/`.
